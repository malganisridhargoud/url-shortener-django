[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Vite is a single-page application: serve index.html for client-side routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
